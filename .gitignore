# === Dependencies / build output ===
**/node_modules/
frontend/.output/
frontend/.nitro/
frontend/.wrangler/
frontend/.vite/
frontend/dist/
**/.turbo/

# === Python ===
**/.venv/
**/.venv311/
**/__pycache__/
*.py[cod]
*.egg-info/

# === Model weights & large data (NOT committed — see README) ===
# Keep the small NOAA CSV (2 MB) + impact model (256 KB) so the hosted backend
# works; still exclude the 90 MB SAR segmenter weights and the 26 GB datasets.
backend/models/*
!backend/models/impact_model.joblib
backend/data/*
!backend/data/oil_spill_master.csv
*.pt
*.pth
*.onnx

# === Databases ===
*.db
*.db-wal
*.db-shm

# === Secrets / env ===
.env
.env.*
!.env.example

# === OS / editor ===
.DS_Store
Thumbs.db
*.log
